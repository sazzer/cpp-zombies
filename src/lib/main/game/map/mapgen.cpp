#include "game/map/mapgen.h"
#include <vector>
#include <iostream>

namespace Game {
    namespace Map {
        /**
         * Generate a new map
         * @param map The map to populate
         */
        void MapGenerator::generate(Map& map) {
        }
    }
}

